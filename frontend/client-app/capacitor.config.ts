import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lynqer.app',
  appName: 'appurape',
  webDir: 'dist/iquitos-delivery-client/browser'
};

export default config;
